<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.baseball.domain.stadium.StadiumDao">

<select id="findAll"
		resultType="site.metacoding.baseball.web.dto.response.stadium.MainDto">
	select *
	from stadium
	ORDER BY id DESC
</select>

<insert id="insert">
	insert into stadium(stadiumname, location, createdAt) 
	VALUES(#{stadiumname}, #{location}, NOW())
</insert>

<select id="findById"
		resultType="site.metacoding.baseball.web.dto.response.stadium.MainDto">
	select *
	from stadium
	WHERE id = #{id}
</select>



</mapper>